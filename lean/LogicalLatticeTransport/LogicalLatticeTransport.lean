import RequestProject.LogicalLatticeTransport.BW2Transport
import RequestProject.LogicalLatticeTransport.BW3Transport

/-!
# Logical-lattice general-Clifford transport (aggregator)

This module bundles the `n = 2` and `n = 3` general-Clifford transport results
closing Theorem 17.6 (`thm:av-logical-lattice`) in kernel form at small `n`,
without the external Kliuchnikov–Schönnenbeck citation.

* `RequestProject.LogicalLatticeTransport.BW2Transport` and
 (`n = 2`).
* `RequestProject.LogicalLatticeTransport.BW3Transport`
 (`n = 3`).
-/
