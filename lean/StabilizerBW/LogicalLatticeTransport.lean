import StabilizerBW.LogicalLatticeTransport.BW2Transport
import StabilizerBW.LogicalLatticeTransport.BW3Transport

/-!
# Logical-lattice general-Clifford transport aggregator

This module bundles the `n = 2` and `n = 3` general-Clifford transport results
closing the logical-lattice theorem in kernel form at small `n`,
without the external Kliuchnikov–Schönnenbeck citation.

* `StabilizerBW.LogicalLatticeTransport.BW2Transport` — the `n = 2` case.
* `StabilizerBW.LogicalLatticeTransport.BW3Transport` — the `n = 3` case.
-/
